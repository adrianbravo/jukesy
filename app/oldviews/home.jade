#app
  #header
    a#home(href= '#/')
      |jukesy
    a(href= '#/favorites') favorites
    a(href= '#/playlists') playlists
    input#query.right(name= 'jukesy_query', type= 'text', value= '', placeholder= 'search for music', spellcheck= 'false')
    .clear
  #main
  #playlist_wrapper
    #playlist.tracks
  #video_wrapper
    #video

#footer
  .content
    #controls



script#controls-template(type= 'text/template')
  != partial('backbone/_controls')

script#song-info-template(type= 'text/template')
  != partial('backbone/_song_info')

script#song-badge-template(type= 'text/template')
  != partial('backbone/_song_badge')

script#playlists-template(type= 'text/template')
  != partial('backbone/_playlists')

script#playlist-short-template(type= 'text/template')
  != partial('backbone/_playlist_short')

script#home-template(type= 'text/template')
  != partial('backbone/_home')

script#lastfm-search-template(type= 'text/template')
  != partial('backbone/_lastfm_search')

script#tag-template(type= 'text/template')
  != partial('backbone/_tag')

script#album-template(type= 'text/template')
  != partial('backbone/_album')

script#artist-template(type= 'text/template')
  != partial('backbone/_artist')

script#track-template(type= 'text/template')
  != partial('backbone/_track')

script#track-actions-template(type= 'text/template')
  != partial('backbone/_track_actions')

script#lastfm-no-results-template(type= 'text/template')
  != partial('backbone/_lastfm_no_results')

script#lastfm-waiting-template(type= 'text/template')
  != partial('backbone/_lastfm_waiting')

script#playlist-template(type= 'text/template')
  != partial('backbone/_playlist')

script#playlist-empty-template(type= 'text/template')
  != partial('backbone/_playlist_empty')

script(src= 'http://ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js')
script(src= 'http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.5/jquery-ui.min.js')
- each script in scripts
  script(src= '/' + script)


